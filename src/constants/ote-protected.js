/** Дата автоудаления в TeamCity для защищённой OTE (UTC, календарь). */
export const OTE_PROTECT_DELETE_DATE_YMD = '2099-12-31'

/** После снятия защиты новая дата удаления — через столько календарных дней от сегодня (UTC). */
export const OTE_UNPROTECT_DEFAULT_DELETE_DAYS = 7
